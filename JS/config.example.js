// ===============================================================
// App Configuration (EXAMPLE)
// ===============================================================
// 1) Copy this file and rename the copy to "config.js"
// 2) Fill in your own Supabase project URL and anon/public key
//    (Project Settings > API in your Supabase dashboard)
// 3) "config.js" is ignored by Git (see .gitignore) and will
//    NOT be uploaded to GitHub — keep your real keys local.
// ===============================================================

window.APP_CONFIG = {
  SUPABASE_URL: "YOUR_SUPABASE_PROJECT_URL",
  SUPABASE_ANON_KEY: "YOUR_SUPABASE_ANON_PUBLIC_KEY"
};
